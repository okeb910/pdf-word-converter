# 更新说明

## v0.5.1（2026-07-28）

### PDF → Word 精度

- 新增后台复杂版式预检，识别线框表格、按文字行列对齐形成的无边框表格、可选择文字、PDF 表单控件、复选/勾选符号、Symbol/Wingdings 字体和大量矢量路径。
- 新增独立保守检测链，直接检查重复对齐的文字列和横纵线网格；主表格提取器返回空结果但版面仍疑似表格时，只要可编辑引擎可用就继续转换，并在完成弹窗要求人工核对，不再因为检测不确定而直接不给输出。
- 预检在批次工作线程逐文件检查全部页面，窗口和安全取消保持可响应。20 页以内继续执行逐页多策略表格提取；超过 20 页的长文档改为全页快速文字、字体映射、勾选和版面风险扫描，不再逐页重复执行高成本表格重建。取消等待状态每秒显示已等待时间；Word 已进入当前文件的打开或保存阶段时仍等待该文件结束，不强杀 Office。
- 只要 PDF 含任何可选择文字，就禁止整页图片转换和 LibreOffice 图片回退；图片模式不会被静默选择，只允许用户明确选择或在提示中确认，并且预检成功证明没有可选择文字的扫描件或纯图形文件。
- 含可选择文字的表格、表格单元格提取不确定、疑似表格区域和长文档均锁定到 Word/LibreOffice 可编辑引擎；手动选择图片模式也不能绕过。源结构无法建立可靠校验基线时保留可打开结果并在完成后警告，没有可编辑引擎时仍失败而不是生成照片。
- 混合批次改为逐文件选择引擎；扫描件或纯图形文件选择图片时，不会再把同批可编辑文字或表格一起切换。
- 修正精细线框网格选择：当 PDF 相邻行的纵向边界仅有 2-3 pt 偏移、但偏移位置没有任何独立单元格时，不再把这些微小偏移扩展成“幽灵列”；只有窄列确实拥有独立单元格证据时才采用更细网格。
- 新增事务式 DOCX 表格拓扑修复：记录源 PDF 的精确矩阵、单元格行列跨度和列宽几何；Word 将一个源单元格拆成多个相邻格时，仅在几何、逐格文字、样式和外边框能够唯一证明归属时合并。存在歧义、复杂对象、样式冲突或边界冲突时失败关闭，不猜测修复。
- 拓扑修复现在也能在源 PDF 证据唯一时拆解 Word 错误生成的纵向聚合单元格；复制文字时会移除 `lastRenderedPageBreak` 这类非语义缓存标记，其他无法证明安全的复杂 run 内容仍会拒绝处理。
- 新增源驱动的逐格边框修复：记录 PDF 每个逻辑单元格的 top/left/bottom/right 可见描边；即使 Word 拓扑已经正确，只要缺边或写成 `nil` 仍会修复。真实纵向合并只保留外边，内部边明确为无边框；源 PDF 确认无边框的表格不会被强制套用全网格。
- 对 20 页以内且源表格基线完整的文档启用 DOCX 硬门禁：文件必须是可正常打开的完整 Word 包；源表与 Word 表格的数量、顺序、行列矩阵、独立单元格数、每格 `rowSpan`/`gridSpan` 拓扑、逐格规范化文字、逐格四边状态和总表格字符数必须完全一致。少边、多边或文字少一个、错一格都会拒绝输出。源基线不可靠或长文档快速预检时，严格校验差异改为完成警告并保留可打开结果。
- 新增可见性与伪造防线，拒绝全页大图叠加伪表格、隐藏/近白/透明/异常小字号文字、文字错格或换格、错误合并、不可证明在页面中可见的表格，以及受保护或锁定内容。
- PDF 预检执行异常仍采用安全失败；预检结果绑定源文件身份，文件在排队期间被替换或改写时该项停止并要求重新加入。转换结果先写同目录私有 `.partial` 文件；可靠基线下未通过硬门禁时清理或隔离失败输出，源基线不可靠的提醒模式则在确认 DOCX 可正常打开后发布结果，并记录具体差异。
- 可编辑表格含 Widget、勾选、疑似矢量勾选、Symbol/Wingdings 字体、U+FFFD 替换字符或私用区字符时，因为无法逐项证明状态不变而进入提醒模式：继续使用可编辑引擎，保留可打开结果，并在批次完成时显示中文质量警告；不提供静默整页图片回退。
- 停止默认执行旧版 DOCX 猜测性自动后处理。对已经通过精确拓扑门禁的单个近满页大型表格，只进行有证据约束的版式修复：清除系统性重复的文字方框、在所有行均有有效显式高度时使用固定行高，并收紧页边距和单元格上下内边距；孤立的合法文字边框不会被删除。
- 保留 `fix_converted_docx()` 兼容入口，但 v0.5.1 中它是无损 no-op，不再重写文件内容。

### 外观优先图片模式

- 默认继续以约 300 DPI 渲染 PDF 页面，显式使用 RGB、关闭 alpha，并在 DOCX 中设置“不自动压缩图片”。
- 新增长边 6000 像素和单页 3000 万总像素双重限制，异常大页面会自适应降低 DPI；即使 1 DPI 仍超限时明确拒绝，防止内存失控。
- 页面超过 Word 22 英寸限制时按比例缩小纸张和整页图片，不裁切内容。
- PDF 句柄改为在 `finally` 中关闭；空 PDF 会显示明确错误，不再生成空白 Word。

### 回归验证

- 增加匿名动态 PDF 测试，覆盖中文空格路径、线框与无边框表格同页、单行/短无边框表格、页眉页脚和冒号标签防误判、普通段落防误判、可选择文字分类、矢量勾选、Widget、Symbol 字体、页数扫描上限和异常清理。
- 增加图片型和语义表格 DOCX 结构测试，验证图片结果被拒绝、真实表格通过、`gridSpan` 有效列数、`vMerge` 独立单元格、全行合并伪表格、表格内外锁定/受保护/空表格被拒绝、逐表文字与结构绑定比对，以及 RGB PNG、无 alpha 和 DPI 双重限制。
- 增加逐项转换策略测试，覆盖手动图片模式保护、显式图片确认、矛盾策略最后防线、混合批次、全部文件/页面预检、长文档快速预检、预检失败关闭、源文件 SHA-256 身份检测、表格+Widget、取消竞态、LibreOffice 禁止图片回退、逐表精确矩阵/跨度/文字门禁，以及疑似表格、可疑字体和严格校验差异保留输出后统一警告。
- 新增表格拓扑、逐格边框修复和硬门禁专项测试，覆盖安全合并、错误纵向聚合拆解、旋转页短边框、无边框表格、纯填充图形防误判、纵向合并内部边、缺边/多边拒绝、分页缓存标记、歧义拒绝、样式/边框冲突、复杂对象、精确单元格跨度、逐格文字、协作取消、OOXML 命名空间保留，以及事务写入失败不替换原文件。当前完整测试结果为 `289 passed, 1 skipped`。
- 使用真实密集矢量成绩表完成本地端到端验证：源表和修复结果均为 `33×26`，共 515 个逻辑单元格、516 个原始 `<w:tc>`、1075 个表格文字字符和 2060 条逻辑外边，唯一真实纵向合并是底部跨两行的“其中包括”单元格；输出含 1 个真实可编辑表格、0 张图片，单元格跨度、逐格规范化文字和逐格四边状态与源证据完全一致。
- 同一真实样本已验证 2-3 pt 边界偏移不会再产生幽灵列，Word 错误纵向聚合可按源证据恢复，原先缺少的 155 个直接单元格侧边已全部补齐；修复后的 DOCX 可由 Microsoft Word 重新打开并导出为单页 A4 横向 PDF，放大检查三个课程区块及底部合并格均未发现漏线、漏字、裁切、黑块或越界文字。
- 使用 225 页真实年度报告完成长文档回归：旧版完整预检约 85.8 秒，快速预检扫描全部页面约 0.6 秒；Word 原生转换约 26.6 秒。源 PDF 有 168,956 个可选择非空字符，DOCX 有 162,655 个可见非空字符，重新导出后由 225 页重排为 259 页，因此结果保留但完成弹窗明确列出全文差异并要求人工核对。259 页渲染检查无黑页、无触边裁切，但有 9 页仅剩页码或极少内容。
- 正式发布文件 `PDF-Word-PPT-Converter-v0.5.1-Portable-x64.exe` 大小 46,679,665 字节，SHA-256 `a42e0971722fe73e0f72612803d343f5ba73916702bf112d648a99a355393192`。已核对为 x64 Windows GUI、版本 `0.5.1.0`，中文空格路径约 5.13 秒出现窗口，环境检测约 133 ms，Windows Defender 自定义扫描 0 检测。

### 已知限制

- Word/LibreOffice 可编辑模式会把可选择文字和线条重建为 Word 表格，但 PDF 已丢失的合并关系等语义仍可能导致列宽、行高或分页变化。
- 表格预检同时使用主表格提取和独立的重复文字列/网格检查。主检测未确认、异常编码、复杂对象、不规则布局或长文档快速预检无法建立完整源基线时，会保留可打开结果并弹出质量提醒，不代表结果通过严格保真验证；源基线可靠时仍执行严格门禁。
- 只要文件含任何可选择文字，即使未明确识别为表格或表格提取结果不确定，也禁止整页图片和图片回退；预检执行异常、源文件被改写或没有可编辑引擎时仍会直接失败。
- 外观优先模式仅在用户明确选择或确认后，用于预检成功且完全没有可选择文字的扫描件或纯图形文件；它不会被静默用作失败兜底，生成的文字和图形不能逐项编辑，也不执行 OCR。
- macOS 仍为源码预览；Windows v0.5.1 提供免 Python 单文件便携版。

## v0.5.0（2026-07-27）

### Windows 便携版

- 继续采用单文件便携版，目标电脑不需要安装 Python，也不会在启动时运行 pip。
- 启动检测改为并行浅检测：只检查 Office 注册和 LibreOffice 路径，不再为了检测而启动 Word、PowerPoint 或 LibreOffice。
- 本机测试中，外部引擎总检测时间由约 17 至 20 秒降至约 0.1 至 0.2 秒；不同电脑会因系统环境而有所差异。
- Word、PowerPoint 和 LibreOffice 改为首次实际转换时验证；原生 Office 转换失败时会清理半成品并自动回退 LibreOffice。
- 修复结构化引擎状态被错误当作“可用”的问题，避免缺失、启动失败或超时的引擎进入转换流程。
- GitHub 下载附件使用清晰的英文文件名，避免平台自动删除中文后出现无法辨认的点号文件名；Release 页面保留中文标签。
- 保留 PDF→Word、Word→PDF、PDF→PPTX、PPT/PPTX→PDF、批量队列、拖放、自定义输出目录、防覆盖命名和安全取消。

### macOS 源码预览

- 同一套源码新增 macOS 13+、Apple Silicon 和 Intel 的平台适配。
- Word→PDF 与 PPT/PPTX→PDF 可通过 Microsoft Office AppleScript 执行，并在不可用时回退 LibreOffice。
- PDF→Word 默认使用内置整页图片模式；PDF→PPTX 继续使用内置高清图片模式。
- 新增 `启动工具.command` 和固定版本的 `requirements-macos.txt`，首次运行创建项目内虚拟环境，依赖未变化时不重复安装。
- TkDND 加载失败时退化为普通 Tk 窗口，不阻止通过按钮选择文件。
- macOS Office 转换只关闭本工具打开的文档，不退出用户原本运行的 Word 或 PowerPoint。
- 新增 Apple Silicon 与 Intel 的 GitHub Actions 源码测试配置。

### 可靠性与发布

- 新增跨平台 `PlatformServices`、`ConversionBackend` 和 `EngineStatus`，统一平台行为和错误状态。
- Windows 与 macOS 的 Office/LibreOffice 回退前都会删除当前转换产生的不完整目标文件。
- 改善 Word COM 异常清理，确保文档、Office 实例和 COM 引用在失败路径中释放。
- 日志继续写入用户目录；启动错误使用可读的图形提示，不依赖控制台窗口。
- Windows 便携版版本资源、manifest、依赖清单和第三方许可证更新为 v0.5.0。
- 发布包包含 AGPL-3.0 主许可证、第三方组件声明和完整第三方许可文本。

### 验证结果

- Windows 自动化测试：105 项通过，1 项仅因测试机未开放符号链接权限而跳过。
- Windows 实际验证：Word PDF↔DOCX、PowerPoint PPTX→PDF、LibreOffice DOCX/PPTX→PDF，以及中文和空格路径。
- Windows 便携版已验证 x64 架构、无控制台窗口、中文界面、正常关闭、无残留进程和 SHA-256 一致性。
- Windows Defender 对最终便携版扫描未发现威胁。

### 已知限制

- macOS 当前仍是源码预览，没有 `.app` 或 `.dmg`，也尚未在真实 Mac 上完成 Office Automation、Finder 拖放和端到端转换验收。
- Windows 便携版尚未数字签名，SmartScreen 可能显示“未知发布者”。
- PDF 图片模式以外观还原为目标，生成的 Word/PPT 内容不能逐项编辑。
- LibreOffice 的 PDF→Word 兼容性有限，复杂文件需要人工核对。
- Microsoft Office 转换仍需要用户自己的有效许可证和账号。

## v0.4.1

- Windows 单便携版发布，加入拖放目标区和并行环境检测。
- 修正便携版中文文件名、说明文件编码、许可证和 GitHub 发布内容。
